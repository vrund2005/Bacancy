## What happens on page refresh?
### When we refresh the page , still both the values are stored in the browser storage
---
## What happens when the tab is closed?
### When we close the tab and reopen that , and when we press the load button without entering new text, the localstorage value logs properly but the session storage value shows null that means the session storage holds the value only when the tab is active, when you close the tab the values disappaers
---

## another thing i noticed during this practical that i save a text and when i create a new tab and retype the text , and go back to the previous tab , the localsession is overwrite by the new value but the session storage remains the same for that specific tab.
